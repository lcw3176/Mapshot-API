version: '3'

services:
  mapshot-notice:
    build:
      context: ./
      dockerfile: mapshot-notice/Dockerfile
    #    deploy:
    #      resources:
    #        limits:
    #          cpus: '0.10'
    #          memory: 128M
    ports:
      - 8080:8080
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '10'
    env_file:
      - notice-env.env
    restart: always

#  mapshot-image:
#    build:
#      context: ./
#      dockerfile: mapshot-image/Dockerfile
#    deploy:
#      resources:
#        limits:
#          cpus: '0.90'
#    #          memory: 700M
#    ports:
#      - 8000:8000
#    logging:
#      driver: 'json-file'
#      options:
#        max-size: '10m'
#        max-file: '10'
#    restart: always





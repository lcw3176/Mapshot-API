version: '3'

services:
  mapshot-notice:
    build:
      context: mapshot-notice
      dockerfile: Dockerfile
    #    deploy:
    #      resources:
    #        limits:
    #          cpus: '0.10'
    #          memory: 128M
    ports:
      - 8080:8080
    env_file:
      - notice-env.env
    restart: always

  mapshot-image:
    build:
      context: mapshot-image
      dockerfile: Dockerfile
    deploy:
      resources:
        limits:
          cpus: '0.80'
    #          memory: 700M
    ports:
      - 8000:8000
    restart: always




